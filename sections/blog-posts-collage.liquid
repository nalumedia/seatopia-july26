<style>
  #shopify-section-{{ section.id }} {
    --section-padding-top: {{ section.settings.padding_top }}px;
    --section-padding-bottom: {{ section.settings.padding_bottom }}px;
    {%- render 'section-variables', section: section -%}
  }
</style>

{{ 'custom-blog.css' | asset_url | stylesheet_tag }}
<script src="{{ 'custom-blog.js' | asset_url }}" defer></script>

{%- if section.index > 2 -%}
  <link rel="stylesheet" href="{{ 'blog.css' | asset_url }}" media="print" onload="this.media='all'">
  <noscript>{{ 'blog.css' | asset_url | stylesheet_tag }}</noscript>
{%- else -%}
  {{ 'blog.css' | asset_url | stylesheet_tag }}
{%- endif -%}

{%- assign blog = section.settings.blog -%}
<div class="section section--padding{% if section.settings.divider %} section--divider{% endif %}{% if section.settings.rounded %} section--rounded relative{% endif %}"
  {% if section.settings.section_id != blank %}
    id="{{ section.settings.section_id }}"
  {% endif %}

>
  <div class="page-width{% if section.settings.full_width %} page-width--full{% endif %} relative">
   

    
    {%- capture view_all -%}
      {%- if section.settings.show_view_all and blog != blank and section.settings.post_limit < blog.articles_count -%}
        <p>
          <a class="button button--secondary icon-with-text" href="{{ blog.url }}" is="hover-link">
            <span class="btn-fill" data-fill></span>
            <span class="btn-text">
              {%- liquid
                render 'icon', icon: 'newspaper'
                echo 'sections.blog_posts.view_all' | t
              -%}
            </span>
          </a>
        </p>
      {%- endif -%}
    {%- endcapture -%}
    {%- render 'section-heading', section: section, section_button: view_all -%}

{% if section.settings.show_tag_filter %}


  
  
         {% comment %}     <div class="hidden md:block">
            <ul class="subcollections inline-flex flex-wrap justify-center max-w-full">
              <li class="flex items-center justify-center relative{% if current_tags == blank %} active{% endif %}">
                <a href="{{ blog.url }}" class="subcollection flex items-center relative leading-tight text-lg-2xl">
                  <span class="btn-text">{{ 'blogs.article.all_tags' | t }}</span>
                </a>
              </li>
              {%- for tag in blog.all_tags -%}
                <li class="flex items-center justify-center relative{% if current_tags contains tag %} active{% endif %}">
                  <a href="{{ blog.url }}/tagged/{{ tag | handleize }}" class="subcollection flex items-center relative leading-tight text-lg-2xl">
                    <span class="btn-text">{{ tag }}</span>
                  </a>
                </li>
              {%- endfor -%}
            </ul>
          </div>

          <div class="blog-filter field md:hidden">
            <div class="inline-block relative">
              <label for="TagFilter" class="sr-only">{{ 'blogs.article.filter' | t }}</label>
              <select id="TagFilter" class="select" is="tag-filter">
                <option value="{{ blog.url }}">{{ 'blogs.article.all_tags' | t }}</option>
                {%- for tag in blog.all_tags -%}
                  <option value="{{ blog.url }}/tagged/{{ tag | handleize }}" {% if current_tags contains tag %}selected{% endif %}>{{ tag }}</option>
                {%- endfor -%}
              </select>
              {%- render 'icon', icon: 'chevron-up', size: 'sm', class: 'absolute pointer-events-none' -%}
            </div>
          </div>  {% endcomment %}



{% comment %} Start custom tag filters and short {% endcomment %}



          {%- if blog.tags.size > 0 -%}
          {%- render 'cstm-blog-facets-sticky-section',
            results: blogs,
            collapsed: section.settings.filter_collapsed,
            enable_filtering: section.settings.show_tag_filter,
            section_id: section.id
          -%}
        {%- endif -%}

  
              <div class="cstm-filter-section cstm-blog-topbar collection__topbar hidden xl:flex items-center gap-2 desktop">
            {%- if section.settings.show_tag_filter and blog.tags.size > 0 -%}
                {%- render 'cstm-blog-facets-drawer',
                  collapsed: section.settings.filter_collapsed,
                  enable_filtering: section.settings.filter_type,
                  enable_sorting: section.settings.enable_sorting,
                  section_id: section.id,
                  section: section  
                -%}
            {%- endif -%}

            {%- if section.settings.title2 != blank or section.settings.filter2tags != blank or blog.tags.size > 0 -%}
              <div class="hidden md:block">
                <ul class="cstm-blog-tag-filter cstm-test-class subcollections inline-flex flex-wrap max-w-full">
                 {% comment %} <li class="flex items-center justify-center relative{% if current_tags == blank %} active{% endif %}">
                    <a href="{{ blog.url }}" class="subcollection flex items-center relative leading-tight text-lg-xl">
                      <span class="btn-text">{{ 'blogs.article.all_tags' | t }}</span>
                    </a>
                  </li> {% endcomment %}
                  {% assign new_tag = section.settings.filter2tags | split: ',' %}
                  {%- for tag in new_tag -%}
                    {% assign tag_clean = tag | strip %}
                    <li class="flex items-center justify-center relative{% if current_tags contains tag_clean %} active{% endif %}">
                      <a
                    {% comment %}
                        href="{{ blog.url }}/tagged/{{ tag_clean | replace: '&amp;', '-' | handleize }}" {% endcomment %}
                        class="subcollection flex items-center relative leading-tight text-lg-xl" tag-name="{{- tag_clean | downcase | replace: ' ','-'-}}"
                      >
                        <span class="btn-text">
                          {{- tag_clean -}}
                          <span class="count absolute text-xs" >{{ tag_clean.size }}</span></span
                        >
                      </a>
                    </li>
                  {%- endfor -%}
                </ul>
              </div>
            {%- endif -%}

            {%- if section.settings.enable_sorting or blog.tags.size > 0 -%}
              {%- liquid
                assign current_tag = current_tags.first
                if current_tag and section.settings.filter3tags contains current_tag
                  assign sort_by_name = current_tag
                else
                  assign sort_by_name = section.settings.title3
                endif
              -%}
              <div class="facet-sorting inline-flex items-center gap-3" id="SortByContainer">
                <span>{{ 'collections.sorting.sort_by_label' | t }}</span>
                <facet-sort class="sort-wrapper relative">
                  <hover-element class="button button--secondary sort-by whitespace-nowrap overflow-hidden z-3">
                    <span class="btn-fill" data-fill></span>
                    <div class="btn-text">
                      <span class="sort-header flex items-center relative justify-center gap-5 w-full">
                        <span class="sort-selection" id="currentSort">{{ sort_by_name | escape }}</span>
                        <span class="sort-title text-sm uppercase absolute left-0 opacity-0 tracking-widest">
                          {{ 'collections.sorting.sort_button' | t }}
                        </span>
                        <span class="cstm-blog-sort sort-close flex items-center justify-center rounded-full">
                          {%- render 'icon', icon: 'close', size: 'sm' -%}
                        </span>
                      </span>
                      <ul
                        class="sort-listbox text-left grid gap-2 absolute left-0 opacity-0 invisible w-full h-auto"
                        id="sortOptions"
                      >
                        {% assign short_tags = section.settings.filter3tags | split: ',' %}
                        {%- for tag in short_tags -%}
                          {% assign tag_clean = tag | strip %}
                          {%- if current_tags contains tag_clean -%}
                            <li class="flex relative active">
                              <a
                                href="{{ blog.url }}/tagged/{{ tag_clean | replace: '&amp;', '-' | handleize }}"
                                class="subcollection flex items-center relative leading-tight text-lg-xl"
                              >
                                <span class="btn-text">{{ tag_clean }}</span>
                              </a>
                            </li>
                          {%- else -%}
                            <li class="flex relative">
                             {% comment %} <a
                                href="{{ blog.url }}/tagged/{{ tag_clean | handleize }}"
                                class="subcollection flex items-center relative leading-tight text-lg-xl"
                              >
                                <span class="btn-text">{{ tag_clean }}</span>
                              </a> {% endcomment %}
                              <a
                              {% comment %}
                              href="{{ blog.url }}/tagged/{{ tag_clean | replace: '&amp;', '-' | handleize }}" {% endcomment %}
                              class="subcollection flex items-center relative leading-tight text-lg-xl" tag-name="{{- tag_clean | downcase | replace: ' ','-'-}}"
                              >
                              <span class="btn-text">
                                {{- tag_clean -}}
                              </span>
                              </a>
                            </li>
                          {%- endif -%}
                        {%- endfor -%}
                      </ul>
                    </div>
                  </hover-element>
                </facet-sort>
              </div>
            {%- endif -%}
          </div>
    {% comment %} End custom tag filters and short {% endcomment %}
{% endif %}

          <div class="cstm-selected-tag-section cstm-selected-tag">
            <ul class="list-unstyled current-tags">
              {% for tag in current_tags %}
                <li class="current--pill-new new-remove-tag" data-tag="{{ tag | handle }}">
                
                  {{ tag | link_to_remove_tag: tag }}
                  <svg width="8" height="8" viewBox="0 0 8 8" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path fill-rule="evenodd" clip-rule="evenodd" d="M0.646446 1.35355C0.451184 1.15829 0.451184 0.841709 0.646446 0.646447C0.841709 0.451185 1.15829 0.451185 1.35355 0.646447L4 3.29289L6.64645 0.646447C6.84171 0.451185 7.15829 0.451185 7.35355 0.646447C7.54882 0.841709 7.54882 1.15829 7.35355 1.35355L4.70711 4L7.35355 6.64645C7.54882 6.84171 7.54882 7.15829 7.35355 7.35355C7.15829 7.54882 6.84171 7.54882 6.64645 7.35355L4 4.70711L1.35355 7.35355C1.15829 7.54882 0.841709 7.54882 0.646447 7.35355C0.451185 7.15829 0.451185 6.84171 0.646447 6.64645L3.29289 4L0.646446 1.35355Z" fill="#53555C"></path>
                  </svg>
                </li>
              {% endfor %}
            </ul>
          </div>

    

    {%- if blog != blank -%}
      {%- liquid
        assign articles_count = blog.articles_count
        if section.settings.post_limit < blog.articles_count
          assign articles_count = section.settings.post_limit
        endif
      -%}


      
      <slider-element class="grid slider" selector=".card-grid>.card">
        <div class="blog-grid blog-collage with-only{{ articles_count }} card-grid mobile:card-grid--1 grid">
          {%- for article in blog.articles limit: section.settings.post_limit -%}
            {%- if forloop.first -%}
              {%- capture sizes -%}(max-width: 1023px) 100vw, calc(min(100vw, {{ settings.page_width }}px) * 0.65){%- endcapture -%}
            {%- else -%}
              {%- capture sizes -%}(max-width: 639px) 100vw, (max-width: 1023px) calc(100vw / 2 - 40px), (max-width: 1535px) calc(min(100vw, {{ settings.page_width }}px) / 3 - 40px), calc(min(100vw, {{ settings.page_width }}px) / 3 - 80px){%- endcapture -%}
            {%- endif -%}
            {%- render 'article-card',
              article: article,
              blog: blog,
              sizes: sizes,
              image_ratio: section.settings.image_ratio,
              show_tags: section.settings.show_tags,
              show_date: section.settings.show_date,
              show_author: section.settings.show_author,
              show_comments: section.settings.show_comments,
              show_excerpt: section.settings.show_excerpt,
            -%}
          {%- endfor -%}
        </div>


  <h3 class="no-data-page" style="text-align:center; display:none">No Blog Posts Found</h3>


        
      </slider-element>
    {%- else -%}
      <div class="rte">
        {{- 'onboarding.no_content' | t -}}
      </div>
    {%- endif -%}


          
  </div>
</div>


          <script> 
$(document).ready(function () {
    let selectedTags = [];

    // Function to fetch data based on the URL
    function getData(url) {
        $('.blog-grid.blog-collage').addClass('loading');
        var urlnew = url.indexOf('view=') > -1 ? url :
                     url.indexOf('?') > -1 ? url + '&view=recepies-data' :
                     url;
        $.ajax({
            url: urlnew,
            type: 'GET',
            dataType: 'html',
            async: false,
            success: function (data) {
                $('.blog-grid.blog-collage').html($('.blog-grid.blog-collage', data).html());
                $('.blog-grid.blog-collage').removeClass('loading');
                $('.current-tags').html($('.current-tags', data).html());
                // history.pushState({}, "", url); 
            },
            error: function (request, error) {
                console.log("Request Error: " + JSON.stringify(request));
            }
        });
    }

    // Function to update selected tags based on active filters
    function updateSelectedTags() {
        selectedTags = [];
        $('.sort-listbox li.active a, .cstm-test-class li.active a, .cstm-blog-tag-filter-drawer  li.active a, .cstm-blog-tag-filter-sticky li.active a, .cstm-mb-top-filter li.active a, .cstm-mb-top-filter-middle li.active a').each(function () {
            const tag = $(this).attr('tag-name');
            if (tag) {
                selectedTags.push(tag);
            }
        });
    }

    // Function to handle click on filter list items
    function handleClick(containerSelector) {
        $(document).on('click', containerSelector + ' li a', function (e) {
            e.preventDefault();
            $(containerSelector + ' li').removeClass('active');
            $(this).closest('li').addClass('active');
            updateSelectedTags();
            const tagString = selectedTags.join('+');
            const url = '/blogs/{{ blog.handle }}/tagged/' + tagString;
            setTimeout(function () {
                $('.current-tags').show();
            }, 500);
            console.log("Generated URL: ", url);
            getData(url);
        });
    }

    // Handle clicking on tags in various filter lists
    handleClick('.sort-listbox');
    handleClick('.cstm-test-class');
    handleClick('.cstm-blog-tag-filter-drawer');
    handleClick('.cstm-blog-tag-filter-sticky');
    handleClick('.cstm-mb-top-filter');
    handleClick('.cstm-mb-top-filter-middle');
  

    // New functionality: Handle removing a selected tag by clicking on the 'X' (close button)
   // New functionality: Handle removing a selected tag by clicking on the 'X' (close button)
$(document).on('click', '.current--pill-new a', function (e) {
    e.preventDefault();

    const $pill = $(this).closest('li');

    // Safely get the tag from data attribute
    const tagToRemove = $pill.data('tag')?.toLowerCase().trim();

    if (!tagToRemove) {
        console.warn("No tag found to remove.");
        return;
    }

    // Remove the tag from selectedTags array
    selectedTags = selectedTags.filter(function(tag) {
        return tag.toLowerCase().trim() !== tagToRemove;
    });

    // Remove the pill from UI
    $pill.remove();

    // Remove the 'active' class from all filter types that match the tag
    $('li a[tag-name]').each(function() {
        const tagName = $(this).attr('tag-name')?.toLowerCase().trim();
        if (tagName === tagToRemove) {
            $(this).closest('li').removeClass('active');
        }
    });

    // Build new URL
    const tagString = selectedTags.join('+');
    const blogHandle = '{{ blog.handle }}'; // This should be rendered server-side
    const url = selectedTags.length > 0 
        ? `/blogs/${blogHandle}/tagged/${tagString}`
        : `/blogs/${blogHandle}`;

    // Fetch new data
    getData(url);
    console.log("Updated URL:", url);

    // Optional UI updates
    setTimeout(function () {
        $('.current-tags').toggle(selectedTags.length > 0);
    }, 500);
});


});


// $(document).ready(function () {
//     let selectedTags = [];
//     function getData(url) {
//         $('.blog-grid.blog-collage').addClass('loading');
//         var urlnew = url.indexOf('view=') > -1 ? url :
//                      url.indexOf('?') > -1 ? url + '&view=recepies-data' :
//                      url;
//         $.ajax({
//             url: urlnew,
//             type: 'GET',
//             dataType: 'html',
//             async: false,
//             success: function (data) {
//                 $('.blog-grid.blog-collage').html($('.blog-grid.blog-collage', data).html());
//                 $('.blog-grid.blog-collage').removeClass('loading');
//                 $('.current-tags').html($('.current-tags', data).html());
//                 // history.pushState({}, "", url); 
//             },
//             error: function (request, error) {
//                 console.log("Request Error: " + JSON.stringify(request));
//             }
//         });
//     }

//     function updateSelectedTags() {
//         selectedTags = [];
//         $('.sort-listbox li.active a, .cstm-test-class li.active a, .cstm-blog-tag-filter-drawer  li.active a, .cstm-blog-tag-filter-sticky li.active a, .cstm-mb-top-filter li.active a').each(function () {
//             const tag = $(this).attr('tag-name');
//             if (tag) {
//                 selectedTags.push(tag);
//             }
//         });
//     }

//     function handleClick(containerSelector) {
//         $(document).on('click', containerSelector + ' li a', function (e) {
//             e.preventDefault();
//             $(containerSelector + ' li').removeClass('active');
//             $(this).closest('li').addClass('active');
//             updateSelectedTags();
//             const tagString = selectedTags.join('+');
//             const url = '/blogs/{{ blog.handle }}/tagged/' + tagString;
//             setTimeout(function () {
//                 $('.current-tags').show();
//             }, 500);
//             console.log("Generated URL: ", url);
//             getData(url);
//         });
//     }



//     handleClick('.sort-listbox');
//     handleClick('.cstm-test-class');
//     handleClick('.cstm-blog-tag-filter-drawer');
//     handleClick('.cstm-blog-tag-filter-sticky');
//     handleClick('.cstm-mb-top-filter');
// });



</script>     

{% schema %}
{
  "name": "t:sections.blog-posts-collage.name",
  "settings": [
    {
      "type": "text",
      "id": "section_id",
      "label": "Section ID",
      "info":"Please enter a unique value in each section"
    },
    {
      "id": "blog",
      "type": "blog",
      "label": "t:sections.blog-posts.settings.blog.label"
    },
    {
      "type": "range",
      "id": "post_limit",
      "label": "t:sections.blog-posts.settings.post_limit.label",
      "default": 4,
      "min": 2,
      "max": 4,
      "step": 1
    },
    {
      "type": "checkbox",
      "id": "show_view_all",
      "label": "t:sections.blog-posts.settings.show_view_all.label",
      "info": "t:sections.blog-posts.settings.show_view_all.info",
      "default": false
    },




      {
    "type": "header",
    "content": "Filters"
  },
  {
      "type": "text",
      "id": "title1",
      "default":"Types of cooking",
      "label": "Filter 1 Title"
    },
    {
      "type": "html",
      "id": "filter1tags",
      "label": "Filter 1 (options) Tags",
      "info": "Add the options comma (,) seprated like Curdo,Baking"
    },
    {
      "type": "text",
      "id": "title2",
      "default":"Types of cooking",
      "label": "Filter 2 Title"
    },
    {
      "type": "html",
      "id": "filter2tags",
      "label": "Filter 2 (options) Tags",
       "info": "Add the options comma (,) seprated like Curdo,Baking"
    },
    {
      "type": "text",
      "id": "title3",
      "default":"Cooking Time",
      "label": "Filter 3 Title"
    },
    {
      "type": "html",
      "id": "filter3tags",
      "label": "Filter 3 (options) Tags",
       "info": "Add the options comma (,) seprated like Curdo,Baking"
    },


    
    {
      "type": "header",
      "content": "t:sections.global.settings.header__post.content"
    },
    {
      "type": "select",
      "id": "image_ratio",
      "options": [
        {
          "value": "adapt",
          "label": "t:sections.global.settings.image_ratio.options__1.label"
        },
        {
          "value": "square",
          "label": "t:sections.global.settings.image_ratio.options__2.label"
        },
        {
          "value": "portrait",
          "label": "t:sections.global.settings.image_ratio.options__3.label"
        },
        {
          "value": "landscape",
          "label": "t:sections.global.settings.image_ratio.options__4.label"
        },
        {
          "value": "wide",
          "label": "t:sections.global.settings.image_ratio.options__5.label"
        }
      ],
      "default": "landscape",
      "label": "t:sections.global.settings.image_ratio.label"
    },
    {
      "type": "checkbox",
      "id": "show_tags",
      "label": "t:sections.main-blog.settings.show_tags.label",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_date",
      "label": "t:sections.main-blog.settings.show_date.label",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_comments",
      "label": "t:sections.main-blog.settings.show_comments.label",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_author",
      "label": "t:sections.main-blog.settings.show_author.label",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_excerpt",
      "label": "t:sections.main-blog.settings.show_excerpt.label",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_tag_filter",
      "label": "Enable filter tag",
      "default": false
    },
    {
      "type": "paragraph",
      "content": "t:sections.main-blog.settings.paragraph.content"
    },
    {
      "type": "header",
      "content": "t:sections.global.settings.header__heading.content"
    },
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "t:sections.global.settings.heading.label",
      "default": "Blog posts"
    },
    {
      "type": "select",
      "id": "heading_size",
      "options": [
        {
          "value": "title-sm",
          "label": "t:sections.global.settings.heading_size.options__1.label"
        },
        {
          "value": "title-md",
          "label": "t:sections.global.settings.heading_size.options__2.label"
        },
        {
          "value": "title-lg tracking-heading",
          "label": "t:sections.global.settings.heading_size.options__3.label"
        },
        {
          "value": "title-xl tracking-heading",
          "label": "t:sections.global.settings.heading_size.options__4.label"
        }
      ],
      "default": "title-md",
      "label": "t:sections.global.settings.heading_size.label"
    },
    {
      "type": "select",
      "id": "heading_alignment",
      "label": "t:sections.global.settings.heading_alignment.label",
      "options": [
        {
          "value": "text-left md:items-end md:flex-row",
          "label": "t:sections.global.settings.heading_alignment.options__1.label"
        },
        {
          "value": "text-center md:items-center",
          "label": "t:sections.global.settings.heading_alignment.options__2.label"
        },
        {
          "value": "text-right md:items-end md:flex-row-reverse",
          "label": "t:sections.global.settings.heading_alignment.options__3.label"
        }
      ],
      "default": "text-left md:items-end md:flex-row"
    },
    {
      "type": "select",
      "id": "heading_tag",
      "options": [
        {
          "value": "h1",
          "label": "t:sections.global.settings.heading_tag.options__1.label"
        },
        {
          "value": "h2",
          "label": "t:sections.global.settings.heading_tag.options__2.label"
        },
        {
          "value": "h3",
          "label": "t:sections.global.settings.heading_tag.options__3.label"
        },
        {
          "value": "h4",
          "label": "t:sections.global.settings.heading_tag.options__4.label"
        },
        {
          "value": "h5",
          "label": "t:sections.global.settings.heading_tag.options__5.label"
        },
        {
          "value": "h6",
          "label": "t:sections.global.settings.heading_tag.options__6.label"
        },
        {
          "value": "div",
          "label": "t:sections.global.settings.heading_tag.options__7.label"
        },
        {
          "value": "span",
          "label": "t:sections.global.settings.heading_tag.options__8.label"
        },
        {
          "value": "p",
          "label": "t:sections.global.settings.heading_tag.options__9.label"
        }
      ],
      "default": "h2",
      "label": "t:sections.global.settings.heading_tag.label",
      "info": "t:sections.global.settings.heading_tag.info"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "t:sections.global.settings.subheading.label"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "t:sections.global.settings.description.label"
    },
    {
      "type": "header",
      "content": "t:sections.global.settings.header__highlight.content",
      "info": "t:sections.global.settings.header__highlight.info"
    },
    {
      "type": "select",
      "id": "highlighted_text",
      "options": [
        {
          "value": "none",
          "label": "t:sections.global.settings.highlighted_text.options__1.label"
        },
        {
          "value": "text",
          "label": "t:sections.global.settings.highlighted_text.options__2.label"
        },
        {
          "value": "full_text",
          "label": "t:sections.global.settings.highlighted_text.options__3.label"
        },
        {
          "value": "half_text",
          "label": "t:sections.global.settings.highlighted_text.options__4.label"
        },
        {
          "value": "underline",
          "label": "t:sections.global.settings.highlighted_text.options__5.label"
        },
        {
          "value": "stencil",
          "label": "t:sections.global.settings.highlighted_text.options__6.label"
        },
        {
          "value": "scribble",
          "label": "t:sections.global.settings.highlighted_text.options__7.label"
        }
      ],
      "default": "none",
      "label": "t:sections.global.settings.highlighted_text.label"
    },
    {
      "type": "select",
      "id": "highlighted_scribble",
      "options": [
        {
          "value": "circle",
          "label": "t:sections.global.settings.highlighted_scribble.options__1.label"
        },
        {
          "value": "basic-underline",
          "label": "t:sections.global.settings.highlighted_scribble.options__2.label"
        },
        {
          "value": "sketch-underline",
          "label": "t:sections.global.settings.highlighted_scribble.options__3.label"
        },
        {
          "value": "squiggle-underline",
          "label": "t:sections.global.settings.highlighted_scribble.options__4.label"
        },
        {
          "value": "squiggle-underline-2",
          "label": "t:sections.global.settings.highlighted_scribble.options__5.label"
        }
      ],
      "default": "circle",
      "label": "t:sections.global.settings.highlighted_scribble.label",
      "info": "t:sections.global.settings.highlighted_scribble.info"
    },
    {
      "type": "header",
      "content": "t:sections.global.settings.header__colors.content",
      "info": "t:sections.global.settings.header__colors.info"
    },
    {
      "type": "color",
      "id": "color_text",
      "label": "t:sections.global.settings.color_text.label"
    },
    {
      "type": "color",
      "id": "color_background",
      "label": "t:sections.global.settings.color_background.label"
    },
    {
      "type": "color_background",
      "id": "gradient_background",
      "label": "t:sections.global.settings.gradient_background.label"
    },
    {
      "type": "color",
      "id": "color_highlight",
      "label": "t:sections.global.settings.color_highlight.label"
    },
    {
      "type": "color_background",
      "id": "gradient_highlight",
      "label": "t:sections.global.settings.gradient_highlight.label"
    },
    {
      "type": "color",
      "id": "color_button_text",
      "label": "t:sections.global.settings.color_button_text.label"
    },
    {
      "type": "color",
      "id": "color_button_background",
      "label": "t:sections.global.settings.color_button_background.label"
    },
    {
      "type": "color_background",
      "id": "color_button_gradient",
      "label": "t:sections.global.settings.color_button_gradient.label"
    },
    {
      "type": "header",
      "content": "t:sections.global.settings.header__section.content"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "t:sections.global.settings.padding_top.label",
      "default": 72
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "t:sections.global.settings.padding_bottom.label",
      "default": 72
    },
    {
      "type": "checkbox",
      "id": "divider",
      "label": "t:sections.global.settings.divider.label",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "full_width",
      "label": "t:sections.global.settings.full_width.label",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "rounded",
      "default": false,
      "label": "t:sections.global.settings.rounded.label"
    }
  ],
  "presets": [
    {
      "name": "t:sections.blog-posts-collage.presets.name",
      "settings": {
        "blog": "",
        "post_limit": 3
      }
    }
  ],
  "disabled_on": {
    "groups": [
      "footer",
      "header",
      "custom.overlay"
    ]
  }
}
{% endschema %}
