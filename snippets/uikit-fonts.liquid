{%- comment -%}
  UI kit webfonts — TEST PAGE ONLY.

  The kit specifies Crimson Pro LIGHT (300). The theme's header font setting is
  currently crimson_pro_n4 (Regular 400), so Shopify's font_face pipeline does
  not serve a 300 weight. Rather than change the global setting before the
  creative team has signed off on the gold rollout, this page loads the weights
  it needs directly.

  AT SITE-WIDE ROLLOUT: set type_header_font to Crimson Pro Light in theme
  settings and delete this snippet.
{%- endcomment -%}
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Crimson+Pro:ital,wght@0,300;0,400;1,300&family=DM+Sans:ital,opsz,wght@0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&display=swap">
